{
  "name": "code-assignment-frontend-generic",
  "version": "1.0.0",
  "description": "Frontend code assignment project for frontend development candidates.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm --prefix ./server ci && npm --prefix ./client ci",
    "dev:server": "npm --prefix ./server run dev",
    "dev:client": "npm --prefix ./client run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "author": "Dzulqarnain Nasir",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "7.0.0"
  },
  "engines": {
    "npm": ">=8.0.0",
    "node": ">=14.0.0"
  }
}